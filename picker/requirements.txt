# Picker application dependencies
# Core imaging and display
Pillow>=9.0.0
python-dateutil

# Hardware interfaces (Raspberry Pi)
spidev>=3.5; sys_platform == "linux"
Adafruit-MCP3008>=1.5.0; sys_platform == "linux"

# GPIO control for Raspberry Pi (if needed)
RPi.GPIO>=0.7.0; sys_platform == "linux"

# Development and testing
pytest>=6.0.0
